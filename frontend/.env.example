# URL da API Backend (apenas REST, sem frontend embutido)
VITE_API_URL=http://localhost:3000
# Host do portal para branding/login (opcional — padrão: window.location.hostname)
# Use localhost em dev se abrir por 127.0.0.1 ou defina o host de produção:
# VITE_PORTAL_HOST=localhost
# VITE_PORTAL_HOST=mariaborboleta.fernnandez.com
# Produção: sempre com https:// (ex.: https://sua-api.up.railway.app)
